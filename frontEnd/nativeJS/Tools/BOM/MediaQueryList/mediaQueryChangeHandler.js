/**
 * 媒体查询匹配变更处理程序
 * matches 布尔值，true表示匹配，false表示未匹配
 * media  字符串值，表示媒体查询
 * @param  {MediaQueryListEvent} e
 */
const mediaQueryChangeHandler = (e) => {
    if (e.matches) {
        // document匹配媒体查询

    } else {
        // document未匹配媒体查询

    }
};
